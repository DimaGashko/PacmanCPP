#pragma once

#include <string>

class GameInfo {
public:
	GameInfo();
	void update();

	// void updateScore(int score);
	// void updateHealth(int score);
	// void updateStatus(std::string status);
	~GameInfo();
};

